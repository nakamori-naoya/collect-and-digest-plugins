# 作れる型は3種だけ

| 型 | 何を並べるか |
|---|---|
| `period-digest` | 期間 × 全ソース横断の1枚。決定／未決／アクション／動きの4区画 |
| `decision-log` | 決まったことだけを1件1エントリ |
| `open-questions` | 未決の相談事項を、状態と滞留期間つきで |

**これ以外は作れない。** 収集物からチュートリアルや API リファレンスを作ることに意味は無い。
他の型が要るなら、資料を書く段取りを直接呼ぶ。

制限は設定の解決時に機械が検査する。**この文章を読み飛ばしても、設定が外れていれば止まる。**

## 型を固定して渡す

資料化を委譲するときは、この3種のどれかを契約入力の`document_type`として**固定して渡す**。
渡した先が型を選び直したら、制限を外したのと同じである。
渡した型が相手の実装に無ければ、契約入力を受け取った時点で相手が止める。

## 3つの型の使い分け

| 読み手の問い | 型 |
|---|---|
| この期間に何があったか | `period-digest` |
| 何が決まったか。いつ、誰が | `decision-log` |
| いま何が止まっているか。誰待ちか | `open-questions` |

**迷ったら `period-digest`。** 決定だけ・未決だけを切り出すのは、
全体を1枚にしても情報が多すぎるときである。
